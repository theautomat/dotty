/**
 * GameOverOverlay.js - Game over screen (TEMPORARY STUB)
 * TODO: Migrate to React component (Phase 9)
 */

export function initGameOverOverlay() {
  console.log('GameOverOverlay initialized (stub)');
}

export function showGameOverOverlay() {
  console.log('Game Over (overlay disabled during migration)');
}

export function hideGameOverOverlay() {
  console.log('GameOverOverlay hide (stub)');
}
